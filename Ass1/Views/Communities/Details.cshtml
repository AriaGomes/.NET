@model Assign1.Models.Community

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Community</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">

            @Html.DisplayNameFor(m => Model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(m => Model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(m => Model.Budget)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(m => Model.Budget)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
